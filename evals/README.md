# Evaluation Cases

This directory contains evaluation cases for testing the tenant-security Agent Skill.

## How it works

The `cases.json` file defines specific testing scenarios to ensure the skill correctly identifies tenant isolation vulnerabilities, handles state verification, generates valid regression tests, and reports findings accurately.

These cases are used during development and validation to measure the effectiveness of the tool against both vulnerable and secure fixtures.
